# Local environment variables
# Set LOCAL=true for local dev (uses local adapters)
LOCAL=true

# Gemini / Vertex AI configuration (leave blank to skip)
GEMINI_PROJECT=gcp-ch-d-prj-paas-omniuccp
GEMINI_LOCATION=europe-west6
# Either set GEMINI_ENDPOINT to the full resource name, or set GEMINI_ENDPOINT_ID
# GEMINI_ENDPOINT=projects/<PROJECT>/locations/<LOCATION>/endpoints/<ENDPOINT_ID>
GEMINI_ENDPOINT_ID=

# Optional: path to service account JSON for ADC
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Local mode
LOCAL=true
SQLITE_PATH=./data/dev.sqlite
# Optional: use OpenAI for local AI responses
OPENAI_API_KEY=
